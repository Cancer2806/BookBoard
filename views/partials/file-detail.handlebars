<div class="content-box">
  <div class="content-header flex">
    <div class="header-title">
      <h3>{{title}}</h3>
      <p class="italic">{{price}} <span>• (122pp)</span></p>
    </div>
    <div class="header-cover">
      <img src="uploads/img/{{cover_art}}" alt="image" class="cover-thumb" />
    </div>
  </div>

  <div class="content-body">
    <div class="content-nav">
      <p class="content-item"><a href="/profile/{{user_id}}">{{user.first_name}}
          {{user.last_name}}
        </a></p>
    </div>
    <div class="content-nav">
      <p class="content-item">{{category.category_name}}</p>
    </div>
    <div class="content-nav">
      <p class="content-item"><span>{{type.type_name}}</span></p>
    </div>
    <div class="content-nav">
      <button class="read-btn"><a href="/file/{{id}}"></a>Read</button>
    </div>

  </div>
</div>